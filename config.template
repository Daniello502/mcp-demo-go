# MCP Kubernetes Demo Configuration Template
# Copy this file and customize for your environment

# Docker Hub Configuration
DOCKER_USERNAME=yourusername
# Note: You'll need a Personal Access Token (PAT) for Docker Hub authentication
# Create one at: https://hub.docker.com/settings/security

# Anthropic API Key (will be base64 encoded in Kubernetes secret)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Service Endpoints (verified for your cluster)
KIALI_ENDPOINT=http://kiali.istio-system:20001
JAEGER_ENDPOINT=http://tracing.istio-system:80
PROMETHEUS_ENDPOINT=http://prometheus.istio-system:9090
GRAFANA_ENDPOINT=http://grafana.istio-system:3000
