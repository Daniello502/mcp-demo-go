apiVersion: v1
kind: ConfigMap
metadata:
  name: mcp-server-config
  namespace: mcp-demo
  labels:
    app: mcp-server
data:
  kiali-endpoint: "http://kiali.istio-system:20001"
  jaeger-endpoint: "http://jaeger-query.istio-system:16686"
  prometheus-endpoint: "http://prometheus.istio-system:9090"
  grafana-endpoint: "http://grafana.istio-system:3000"
